server {
    listen 80;
    server_name dimigoin.io;

    root /usr/share/nginx/html;
    index index.html;

    location / {
        try_files $uri $uri/ /index.html;
    }

    location ~* \.(js|css|png|jpg|jpeg|gif|svg|ico|woff2?|ttf|eot|otf|mp4|webm)$ {
        expires 10m;
        access_log off;
        add_header Cache-Control "public";
    }
}
